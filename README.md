Test locally using >> pip install -r requirements.txt
Run >> python app.py
Open http://localhost:5000 ,displays= DevOps Demo app is running as expected!
Update app.py for health checks and test locally -Open http://localhost:5000/health, display= {"status":"healthy"}
